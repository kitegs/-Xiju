# 可开关分析能力实施记录

目标：会话级分析能力菜单、任务配置快照、实际后端执行和可查看的组件结果。

- [x] 配置模型与 SQLite 增量迁移；旧客户端沿用默认行为。
- [x] 计划器依据配置插入受控组件，禁止模型越过关闭开关。
- [x] 深入分析：至多 1–3 轮，模型根据证据选择固定只读诊断；不开放动态 SQL/Python 权限。
- [x] 内容审阅：本地检查及可选模型审阅，只输出意见；服务商 Usage 先于输出解析记账。
- [x] 图表排版：对本次新报告应用本地布局；多方案仅输出可比较的章节顺序提案。
- [x] 输入框菜单、快速/完整预设、结果与 Token 明细。
- [x] 验证配置持久化、关闭开关、冻结任务配置、未知工具拒绝和主流程。

多表关联依赖尚未实现的关联引擎，本次以不可用状态展示。数字证据校验、权限与确认保存不受组件开关影响。多方案不自动覆盖现有报告。

验证：后端 41 项通过，Playwright 3 条主流程通过，Vite 构建通过。模型路径使用测试替身，真实 DeepSeek 联网效果仍待单独验收。
